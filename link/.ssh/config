Host *

# Automatically add to known_hosts, but prevent connections when a host key changes
StrictHostKeyChecking accept-new

# Allow MacOS to use the keychain for SSH key passphrases
Host *
  UseKeychain yes
  AddKeysToAgent yes
  IdentityFile ~/.ssh/id_rsa
